# Elaview Frontend - Docker Environment Template
# Copy this file to .env.docker and fill in values for Docker setup
# Your main app .env file is located in ./frontend/.env (keep using that for development)

# =============================================================================
# DOCKER CONFIGURATION
# =============================================================================
BUILD_TARGET=development
NODE_ENV=development

# =============================================================================
# PORT CONFIGURATION (Docker Services)
# =============================================================================
FRONTEND_PORT=3000

# =============================================================================
# API CONFIGURATION (Docker-specific)
# =============================================================================
# API URL when running full stack with Docker
VITE_API_BASE_URL=http://localhost:3001/api

# =============================================================================
# NOTES
# =============================================================================
# Your application environment variables should remain in:
# ./frontend/.env
#
# This file is only for Docker-specific configuration like:
# - Port mappings for Docker services  
# - API URLs when connecting to backend container
# - Docker build targets
#
# All your existing API keys, secrets, and app configuration
# should stay in ./frontend/.env where they currently are.
#
# When running with Docker Compose, the API URL above will allow
# the frontend to connect to the backend container if both are running.